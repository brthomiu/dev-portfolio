export type ProjectType = {
  name: string;
  images: string[];
  technologies: string[];
  description: string;
  repoLink: string;
  liveLink: string;
};

export type ImagesType = {
  images: string[]
}

export type TechCloudType = {
  technologies: string[]
}